on: push

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: .
        name: Test deployment
        with:
          k8sConfig: ${{secrets.K8S_CONFIG}}
          scriptPath: ./test-deployment.cjs
          secret: ${{secrets.CONFIG_KEY}}
